# Core Architecture - Kubernetes Module

This project demonstrates:

- Namespace isolation
- Deployment-based workload
- ClusterIP service exposure
- Ingress routing using NGINX

Flow:
Ingress → Service → Deployment → Pods
